#!/bin/sh
IGNORE=''''
exec "$(dirname "$0")"/../bin/hl $debug $options -r "$0"
'''

# {!} negate in pattern: highlight lines that do NOT contain "error"
[[rule]]
pattern = '''{!}error'''
line_color = 'bred'
