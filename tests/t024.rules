#!/bin/sh
IGNORE=''''
exec "$(dirname "$0")"/../bin/hl $debug $options -r "$0"
'''

# {#!} combined (flags in reverse order): same semantics as {!#}
# Pattern "h e l l o" strips to "hello"; lines NOT containing "hello" are highlighted
[[rule]]
pattern = '''{#!}h e l l o'''
line_color = 'bred'
