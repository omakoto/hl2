#!/bin/sh
IGNORE=''''
exec "$(dirname "$0")"/../bin/hl $debug $options --width 120 -n -r "$0" -f "$(dirname "$0")"/ascii.txt "$(dirname "$0")"/ascii.txt
'''

[[rule]]
pattern = '''^10 '''
show = true
